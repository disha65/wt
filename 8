<!DOCTYPE html>
<html>
<head>
<title> setB1.html</title>
</head>
<body>
<form action=setB1.php method=get>
<fieldset>
<center>
<b> Enter 1st String:
<input type=text name=s1><br>
<b> Enter 2nd String:
<input type=text name=s2><br>
a.Find whether the small string appears at the start of the string
<input type=radio name=ch value=a><br>
b.Find the position of small string in last stirng
<input type=radio name=ch value=b><br>
c.Compare string with n characters
<input type=radio name=ch value=c><br>

<input type=submit name=submit value=submit>
</fieldset>
</form>
</body>
</html>

<?php
$s1=$_GET['s1'];
$s2=$_GET['s2'];
$ch=$_GET['ch'];
echo"<center><b><br>Entered 1st string:$s1";
echo"<center><b><br>Entered 2nd string:$s2";
switch($ch)
{ case 'a':if(strpos($s1,$s2)==0)
		{ echo"<br><b>\"$s2\"</b> is at the start of <b>\"$s1\"</b>";
		}
		else
		echo"<br><b>\"$s2\"</b> is not at the start of<b> \"$s1\"</b>";
		break;
 case'b':$pos=strpos($s1,$s2);
 	echo"Position of $s2 in $s1=$pos";
 	break;
 	
 case 'c':echo"string compare with n charchters:=".strncasecmp($s1,$s2,4);
 break;
}




from pandas import*
from numpy import*
from sklearn import preprocessing
import scipy.stats as p
d=read_csv('wine.csv')
print('Standardizing data')
x_wine=array(d)
print('Initial mean : ',p.tmean(x_wine).round(2))
print('Initial standard deviation is :',round(x_wine.std(),2))
x_scaled=preprocessing.scale(x_wine)
x_scaled.mean(axis=0)
x_scaled.std(axis=0)
print('\nStandardized Data : \n',x_scaled.round(2))
print('\nStandardized Mean : ',p.tmean(x_scaled).round(2))
print('\nStandardized Deviation : ',round(x_scaled.std().round(2)))
